---
import { getEntry } from 'astro:content';
const { data: content } = await getEntry('sections', 'site-content');
const spa = content.spa;
---

<section class="section spa" id="spa">
  <div class="container">
    <div class="split">
      <div class="split__text">
        <span class="section__label">{spa.label}</span>
        <h2 class="section__title" set:html={spa.title.replace('&', '&amp;<br />')} />
        {spa.paragraphs.map(p => <p>{p}</p>)}
        <div class="spa__options">
          {spa.options.map(opt => (
            <div class="spa__option">
              <h4>{opt.name}</h4>
              <p>{opt.description}{opt.price && <><br /><strong>{opt.price}</strong></>}</p>
            </div>
          ))}
        </div>
      </div>
      <div class="split__media">
        <img src="/images/property/spa-jacuzzi.jpg" alt="Spa privatif — bain nordique" style="width:100%;height:100%;object-fit:cover;border-radius:var(--radius-lg);min-height:400px" loading="lazy" />
      </div>
    </div>
  </div>
</section>

<style>
  .spa { background: var(--c-bg); }
  .spa__options {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    margin-top: var(--space-lg);
  }
  .spa__option {
    padding: var(--space-md);
    background: var(--c-bg-warm);
    border-radius: var(--radius-md);
    border-left: 3px solid var(--c-accent);
  }
  .spa__option h4 {
    font-family: var(--ff-display);
    font-size: var(--fs-md);
    font-weight: 500;
    margin-bottom: 0.3em;
  }
  .spa__option p {
    font-size: var(--fs-sm);
    color: var(--c-text-light);
    margin: 0 !important;
  }
</style>
