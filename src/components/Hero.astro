<header class="hero" id="hero">
  <div class="hero__overlay"></div>
  <div class="hero__content">
    <p class="hero__subtitle">Normandie — Orne</p>
    <h1 class="hero__title">Domaine<br />La Cage aux Oiseaux</h1>
    <p class="hero__tagline">Un lieu vivant, sensible, façonné par la nature<br />et l'univers artistique de Coco Eugène Lacroix.</p>
    <a href="#hebergements" class="btn btn--primary">Découvrir nos séjours</a>
  </div>
  <div class="hero__scroll">
    <span>Défiler</span>
    <div class="hero__scroll-line"></div>
  </div>
</header>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background:
      linear-gradient(170deg, #4a5d3a 0%, #3a4a2f 30%, #2a3a24 60%, #1f2b1b 100%);
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 30% 20%, rgba(196,169,125,.15) 0%, transparent 60%),
      radial-gradient(ellipse at 70% 80%, rgba(107,127,94,.2) 0%, transparent 50%);
  }
  .hero__overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,.15);
  }
  .hero__content {
    position: relative;
    z-index: 1;
    max-width: 800px;
    padding: var(--space-xl);
  }
  .hero__subtitle {
    font-family: var(--ff-body);
    font-size: var(--fs-sm);
    font-weight: 400;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--c-accent-light);
    margin-bottom: var(--space-md);
  }
  .hero__title {
    font-family: var(--ff-display);
    font-size: var(--fs-hero);
    font-weight: 300;
    line-height: 1.05;
    color: var(--c-white);
    margin-bottom: var(--space-md);
  }
  .hero__tagline {
    font-family: var(--ff-display);
    font-size: var(--fs-lg);
    font-weight: 300;
    font-style: italic;
    color: rgba(255,255,255,.8);
    margin-bottom: var(--space-xl);
    line-height: 1.5;
  }
  .hero__scroll {
    position: absolute;
    bottom: var(--space-lg);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
    color: rgba(255,255,255,.5);
    font-size: var(--fs-xs);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    z-index: 1;
  }
  .hero__scroll-line {
    width: 1px;
    height: 40px;
    background: rgba(255,255,255,.3);
    animation: scrollPulse 2s ease-in-out infinite;
  }
  @keyframes scrollPulse {
    0%, 100% { transform: scaleY(1); opacity: .3; }
    50%      { transform: scaleY(1.4); opacity: .8; }
  }
  @media (max-width: 768px) {
    .hero__tagline br { display: none; }
  }
</style>
